const reduceMotion=matchMedia('(prefers-reduced-motion: reduce)').matches;const videos=[...document.querySelectorAll('video[data-src]')];function load(v){if(!v.src){v.src=v.dataset.src;v.load()}}videos.forEach(v=>{v.tabIndex=0;v.addEventListener('click',()=>{load(v);v.paused?v.play():v.pause()});v.addEventListener('keydown',e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();v.click()}})});if(!reduceMotion&&'IntersectionObserver'in window){const io=new IntersectionObserver(es=>es.forEach(e=>{const v=e.target;if(e.isIntersecting&&e.intersectionRatio>=.6){load(v);videos.forEach(x=>{if(x!==v)x.pause()});v.play().catch(()=>{})}else v.pause()}),{threshold:[0,.6,1],rootMargin:'200px 0px'});videos.forEach(v=>io.observe(v))}document.addEventListener('visibilitychange',()=>{if(document.hidden)videos.forEach(v=>v.pause())});
